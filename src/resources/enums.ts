export enum MemberStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    DECEASED = "deceased",
    TERMINATED = "terminated"
}

export enum PdfStatus {
    GENERATE = "Generated",
    PENDING = "Pending",
    FAIL = "Failed",
}

export enum MailStatus {
    SENT = "Sent",
    PENDING = "Pending",
    FAIL = "Failed",
}
